name: test_ar
description: "Flutter AR Demo"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  arcore_flutter_plugin:
    git:
      url: https://github.com/giandifra/arcore_flutter_plugin
      ref: android-upgrading


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  very_good_analysis: ^5.1.0

flutter:
  uses-material-design: true